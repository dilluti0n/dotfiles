{
    "layer": "top",
    "position": "top",
    "modules-left": ["sway/workspaces", "sway/mode", "mpris"],
    "modules-center": ["sway/window"],
    "modules-right": [
        "pulseaudio",
        "custom/bzmenu",
        "network",
        "cpu",
        "memory",
        "battery",
        "tray",
        "clock"
    ],

    "sway/workspaces": {
        "format": "{name}"
    },
    "mpris": {
        "format": "MPRIS {player}",
        "format-paused": "MPRIS {player} (PAUSED)",
        "player-icons": {
            "default": ""
        },
        "status-icons": {
            "playing": "PLAY",
            "paused": "PAUSE",
            "stopped": "STOP"
        },
        "max-length": 25
    },
    "clock": {
        "format": "{:%Y-%m-%d | %H:%M}",
        "tooltip-format": "{:%Y-%m-%d | %H:%M}"
    },
    "cpu": {
        "format": "CPU {usage}%"
    },
    "memory": {
        "format": "MEM {percentage}%"
    },
    "battery": {
        "bat": "BAT0",
        "interval": 5,
        "format": "BAT {capacity}%",
        "format-charging": "CHARGING {capacity}%",
        "format-plugged": "PLUGGED {capacity}%",
        "format-alt": "BAT {time}"
    },
    "network": {
        "format-wifi": "WIFI {essid}",
        "format-ethernet": "ETH {ifname}",
        "format-disconnected": "DISCONNECTED",
        "on-click": "killall -q iwgtk || iwgtk"
    },
    "pulseaudio": {
        "format": "VOL {volume}%",
        "format-muted": "MUTED",
        "on-click": "killall -q pavucontrol || pavucontrol"
    },
    "tray": {
        "icon-size": 15,
        "sapcing": 10
    },
    "custom/bzmenu": {
        "format": "BZMENU",
        "on-click": "bzmenu -l bemenu",
        "tooltip": false
    }
}
